\chapter*{Introduzione} %Se si cambia il Titolo cambiare anche la riga successiva così che appia corretto nell'indice
\addcontentsline{toc}{chapter}{Introduzione} %Per far apparire Introduzione nell'indice (Il nome deve rispecchiare quello del chapter)
\pagenumbering{arabic} % Settaggio numerazione normale

Le vulnerabilità del software sono tra le principali cause di violazione della sicurezza informatica. Tra queste, la vulnerabilità format string è nota da decenni eppure continua a comparire in codice reale: nasce da un errore tecnicamente semplice --- lasciare che l'utente controlli la stringa di formato passata a una funzione come \texttt{printf} --- ma le sue conseguenze possono arrivare fino a leggere informazioni sensibili e scrivere in modo arbitrario nella memoria del programma.

Durante CyberChallenge.IT --- il programma nazionale di addestramento in sicurezza informatica a cui ho preso parte --- ho incontrato per la prima volta i fondamenti della sicurezza del software, un ambito centrale poiché una parte significativa degli attacchi informatici sfrutta proprio difetti di progettazione e vulnerabilità del codice. Tra le vulnerabilità presentate, quella che mi ha colpito di più per la gravità delle sue possibili conseguenze è stata la format string.

Il lavoro studia la vulnerabilità format string attraverso la \texttt{printf} del linguaggio C, la funzione più rappresentativa di questa classe di vulnerabilità. L'architettura di riferimento è x86-64, oggi tra le più diffuse in ambito desktop e server. Il contributo della tesi è duplice: da un lato spiega il meccanismo tecnico alla base della vulnerabilità e lo dimostra concretamente, sviluppando e sfruttando un programma vulnerabile scritto appositamente per lo studio, in un ambiente controllato e riproducibile; dall'altro valuta le protezioni pensate specificamente contro questo tipo di vulnerabilità, verificandone l'efficacia proprio sull'attacco appena sviluppato.

Il Capitolo~\ref{chapter:background} introduce le nozioni tecniche necessarie a comprendere la vulnerabilità: le funzioni variadiche del linguaggio C, il funzionamento della \texttt{printf} e la convenzione con cui i suoi argomenti vengono passati a basso livello sull'architettura x86-64. Il Capitolo~\ref{chapter:fmt_vuln} presenta la vulnerabilità format string vera e propria, contrapponendo un uso corretto e uno vulnerabile della \texttt{printf} e mostrando, su due piccoli programmi di esempio, come da essa si possa sia leggere sia scrivere in memoria. Il Capitolo~\ref{chapter:exploit} combina queste tecniche in un caso pratico completo: mettendosi nei panni di un attaccante che dispone del solo binario compilato, senza codice sorgente, viene sviluppato un exploit che arriva fino all'ottenimento di una shell interattiva. Il Capitolo~\ref{chapter:mitigations} chiude la parte tecnica valutando le protezioni pensate specificamente per la format string, verificandone sperimentalmente l'efficacia sullo stesso attacco del Capitolo~\ref{chapter:exploit} e discutendo infine le pratiche di scrittura del codice che permettono di evitare la vulnerabilità a monte. Seguono le Conclusioni, che riprendono i risultati ottenuti, e l'Appendice~\ref{app:source}, con il codice sorgente completo degli script e del programma discussi nel corso della tesi, a supporto della riproducibilità del lavoro.

I risultati mostrano che nessuna delle protezioni attive di default sul sistema è sufficiente a impedire questo tipo di attacco, e che anche le protezioni pensate specificamente per la format string ne bloccano solo una parte. L'unica difesa realmente efficace resta quindi a monte: un uso corretto del linguaggio e delle sue funzioni, già in fase di scrittura del codice.

I contenuti presentati in questa tesi hanno esclusivamente finalità didattiche e di ricerca in ambito di sicurezza informatica: nessuna delle tecniche descritte è da intendersi come incentivo al loro utilizzo per scopi illeciti.